[project]
name = "llm-server-template-ollama"
version = "0.1.0"
description = "Config-driven Ollama LLM server with MLX backend on Apple Silicon"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
]
